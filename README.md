# EmbedPowerFX-Session
Repo for the "Embedding PowerFX in .NET" Conference session.

The __slides__ folder contains a coy of the slides (PowerPoint and PDF).

The __src__ folder contains the example .NET project.
